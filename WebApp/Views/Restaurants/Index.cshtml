@model IEnumerable<Models.Restaurant>

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome Restaurants</h1>
</div>

<table>
    <thead>
        <tr>
            <td> Restaurant list </td>
        </tr>
    </thead>
    <tbody>
        @foreach (var restaurant in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelRestaurant => restaurant.Name);
                </td>
            </tr>
        }
    </tbody>
</table>